{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the main window: IPC commands and core events only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
